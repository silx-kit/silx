subdir('test')

py.install_sources([
    '__init__.py',
    'parseutils.py',
]
pure: false,    # Will be installed next to binaries
subdir: 'silx/app/utils',  # Folder relative to site-packages to install to
)
