subdir('compare')
subdir('view')
subdir('utils')
subdir('test')

py.install_sources([
    '__init__.py',
    'convert.py',
    'test_.py',
]
pure: false,    # Will be installed next to binaries
subdir: 'silx/app',  # Folder relative to site-packages to install to
)
