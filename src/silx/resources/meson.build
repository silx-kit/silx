subdir('opencl')
subdir('gui')

py.install_sources([
    '__init__.py',
]
pure: false,    # Will be installed next to binaries
subdir: 'silx/resources',  # Folder relative to site-packages to install to
)
