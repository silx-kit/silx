py.install_sources(
   ['cast.cl',
    'histogram.cl',
    'map.cl',
    'max_min.cl'],
  pure: false,   # Will be installed next to binaries
  subdir: 'silx/resources/opencl/image'  # Folder relative to site-packages to install to
)