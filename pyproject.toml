[build-system]
requires = [
    "wheel",
    "setuptools",
    "numpy>=1.12",
    "Cython>=0.21.1"
]
build-backend = "setuptools.build_meta"

[tool.black]
required-version = 23
target-version = ["py37", "py38", "py39", "py310", "py311", "py312"]
safe = true
