[build-system]
requires = [
    "wheel",
    "setuptools",
    "Cython>=0.29.31"
]
build-backend = "setuptools.build_meta"

[tool.black]
required-version = 25
target-version = ["py310", "py311", "py312", "py313"]

[tool.pytest.ini_options]
minversion = "6.0"
